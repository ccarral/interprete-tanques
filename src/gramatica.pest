WHITESPACE = _{" " | "\n"}

int = { ASCII_DIGIT+ }

decl = { "var" ~ nom_var ~  "=" ~ int ~ ";" }
    nom_var = @{ ASCII_ALPHA_LOWER+ }

expr = { decl }

bloque = _{ (expr)* ~ expr?}

prog = _{
    SOI ~
    bloque ~
    EOI
}


