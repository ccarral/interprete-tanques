WHITESPACE = _{" " | "\n"}

int = { ASCII_DIGIT+ }

decl = { var ~ nom_var ~  "=" ~ int ~ ";" }
    var = { "var" }
    nom_var = { ASCII_ALPHA_LOWER+ }

expr = { decl }

bloque = { (expr)* ~ expr?}

prog = _{
    SOI ~
    bloque ~
    EOI
}


