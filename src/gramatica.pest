WHITESPACE = _{" " | "\n"}

int = { ASCII_DIGIT+ }

decl = { "var" ~ nom_var ~  "=" ~ int ~ ";" }
    nom_var = @{ ASCII_ALPHA_LOWER+ }

inst = { decl }

bloque = _{ (inst)* ~ inst?}

prog = _{
    SOI ~
    bloque ~
    EOI
}


